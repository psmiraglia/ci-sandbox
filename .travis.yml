language: python

python:
    - "2.7"

before_install:
    - curl -L https://bootstrap.saltstack.com -o /tmp/bootstrap-salt.sh
    - sudo sh /tmp/bootstrap-salt.sh stable

install:
    - mkdir -p /srv/salt/states
    - cp -r ./formula /srv/salt/states/formula
    - cp .travis/top.sls /srv/salt/states/top.sls
    - cp .travis/minion /etc/salt/minion

script:
    - sudo salt-call --local state.show_highstate
    
